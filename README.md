# TTY-Timed
A TUI timer and stopwatch script made for Linux 

how to use (currently):
<pre> ```bash git clone https://github.com/SlurpyIsADev/TTY-Timed.git 
  cd TTY-Timed 
  gcc tty-timed.c Font/default-font.c -o tty-timed 
  ./tty-timed ``` </pre>

